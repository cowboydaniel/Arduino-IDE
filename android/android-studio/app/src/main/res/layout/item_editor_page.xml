<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:background="@drawable/code_block_background">

    <TextView
        android:id="@+id/line_numbers"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="12dp"
        android:lineSpacingExtra="6dp"
        android:textAppearance="@style/TextAppearance.Material3.LabelSmall"
        android:textColor="@color/arduino_text_secondary"
        android:fontFamily="monospace"
        android:importantForAccessibility="no"
        tools:text="01\n02\n03\n04\n05" />

    <TextView
        android:id="@+id/code_listing"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:lineSpacingExtra="6dp"
        android:textAppearance="@style/TextAppearance.Material3.BodyMedium"
        android:textColor="@android:color/white"
        android:fontFamily="monospace"
        android:contentDescription="@string/editor_content_description"
        tools:text="void setup() {\n  pinMode(LED_BUILTIN, OUTPUT);\n}" />

</LinearLayout>
